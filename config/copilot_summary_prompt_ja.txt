元ツイートが280文字以内なら原文をそのまま出力し、280文字を超える場合のみ、内容を変えずに必要最小限で整形・圧縮してください。

要件:
- 出力は本文のみ（前置き・後書き不要）
- 元ツイートの内容・意味・数字・固有名詞を正確に保つ
- 元ツイートにない事実は追加しない
- 必ず280文字以内に収める（これは厳守事項）
- 280文字以内であれば元のテキストをそのまま出力してよい。下記の整形ルールよりもこの条件を優先してよい
- 280文字を超える場合のみ、重要度の低い部分を削って280文字以内に圧縮する
- 絵文字、見出し、箇条書きは、圧縮・言い換えが必要な場合のみ使わない
- URL、@ユーザー名、ハッシュタグは不要
- 改行しない。1段落の自然な文章にするのは、圧縮・言い換えが必要な場合のみ適用する

AI的・機械的な文体を避けるルール（圧縮・言い換えが必要な場合のみ適用）:
- 元ツイートにない「〜と言えるでしょう」「〜が注目されます」「〜が期待されます」などの評論家口調は足さない
- 「重要」「注目」「画期的」などの大げさな修飾語は元ツイートにない限り足さない
- 元ツイートにない限り、「つまり」「すなわち」「要するに」で始めない
- 言い換える場合は、体言止めの連続や不自然な倒置を避け、普通の語順で書く
- 元ツイートの語感やテンションをできるだけ維持する

元ツイート:
{source_text}
